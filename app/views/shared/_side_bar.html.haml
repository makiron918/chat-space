.chat-side
  .side-header
    .side-header__items
      %p.side-header__items__user-name
        = current_user.name
      %ul.side-header__items__menu
        %li.side-header__items__menu__edit-user
          = link_to "#" do
            = icon('fa', 'edit', class: 'icon')
        %li.side-header__items__menu__new-group
          = link_to edit_user_path(current_user) do
            = icon('fa', 'cog', class: 'icon')
  .groups
    .group
      %p.group__group-name
        test
      %p.group__message
        Hello